When a project specifies a dependency using a local file path, `briefcase dev` now installs that dependency in editable mode. This means changes in the local project will be immediately reflected in the development environment, without needing to reinstall the package.
