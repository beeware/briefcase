The end-of-app lifecycle for a macOS and iOS application now guarantees that the embedded Python interpreter is not finalized before Objective C code has had a chance to autorelease any objects that might be under management. This could lead to segmentation faults on application exit, especially when an Objective C object was orphaned and in a cyclical reference with a Python object.
