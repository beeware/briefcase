Added note in the docs about expired intermediate **Developer ID** Apple certificates resulting in **Developer ID Application** certificates being marked as `"Developer ID Application: [...]" certificate is not trusted` instead of `This certificate is valid` in Keychain Access after installing, and how to fix the issue.
