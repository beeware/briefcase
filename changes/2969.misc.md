When a Windows app fails to run, Briefcase now reports any startup diagnostics the app was able to record. A GUI app on Windows has no console attached, so its output never reaches Briefcase; this meant that an app which failed during startup produced no diagnostic information at all.
