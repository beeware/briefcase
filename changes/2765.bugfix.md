Version number validation has been improved. Dynamic version number specifications (e.g., `{attr: ...}`) that have been historically permitted by tools like `setuptools` are now rejected with a more helpful error message.
