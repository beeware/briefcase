Normalize PEP 621 project names before merging them into Briefcase configuration, warn when legacy configuration uses an invalid project name, and use the app name as the default project name in the new-project wizard.
