Linux system packages (`.deb`, `.rpm` and `.pkg.tar.zst`) can now be signed when building with Docker, using the same GPG signing identity as native builds. The signing identity is exported from the host and imported into the build container for the duration of the signing step.
