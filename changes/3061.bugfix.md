A formal name that contains no valid Python identifier characters, such as one made only of punctuation or an emoji, no longer produces an app with an empty class name; the class name falls back to ``Briefcase``.
